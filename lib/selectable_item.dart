import 'package:flutter/widgets.dart';

class SelectableItem extends StatelessWidget {
  final Widget child;
  const SelectableItem({Key key, this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
